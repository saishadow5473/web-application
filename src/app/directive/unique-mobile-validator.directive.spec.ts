import { UniqueMobileValidatorDirective } from './unique-mobile-validator.directive';

describe('UniqueMobileValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueMobileValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
