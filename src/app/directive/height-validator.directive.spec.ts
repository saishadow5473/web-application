import { HeightValidatorDirective } from './height-validator.directive';

describe('HeightValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
